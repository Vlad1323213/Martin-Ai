# ✅ Правильная настройка Google OAuth Client

## 🎯 Проблема:

Google требует разные форматы для двух полей:
1. **Авторизованные источники JavaScript** - только домен БЕЗ пути
2. **Авторизованные URI перенаправления** - полный путь

---

## ✅ ПРАВИЛЬНО:

### **1. Авторизованные источники JavaScript (Authorized JavaScript origins):**

Вставьте **ТОЛЬКО** домен без пути:

```
http://localhost:3001
```

⚠️ **БЕЗ** `/api/auth/google/callback`  
⚠️ **БЕЗ** слэша в конце  
⚠️ Проверьте что нет пробелов!

---

### **2. Авторизованные URI перенаправления (Authorized redirect URIs):**

Вставьте **ПОЛНЫЙ** путь с callback:

```
http://localhost:3001/api/auth/google/callback
```

✅ С полным путем  
⚠️ Проверьте что нет пробелов в начале или конце!

---

## 📝 Пошаговая инструкция:

### **В форме Google OAuth Client:**

**Поле 1: "Авторизованные источники JavaScript"**
```
Нажмите "+ ADD URI"
Вставьте: http://localhost:3001
(только домен, без /api/...)
```

**Поле 2: "Авторизованные URI перенаправления"**  
```
Нажмите "+ ADD URI"
Вставьте: http://localhost:3001/api/auth/google/callback
(полный путь с callback)
```

**Нажмите "CREATE"**

---

## 🔍 Частые ошибки:

### ❌ Неправильно:

**JavaScript origins:**
```
❌ http://localhost:3001/api/auth/google/callback  (есть путь)
❌ http://localhost:3001/                           (есть слэш)
❌  http://localhost:3001                           (пробел в начале)
```

**Redirect URIs:**
```
❌ http://localhost:3001                            (нет пути)
❌  http://localhost:3001/api/auth/google/callback (пробел в начале)
```

### ✅ Правильно:

**JavaScript origins:**
```
✅ http://localhost:3001
```

**Redirect URIs:**
```
✅ http://localhost:3001/api/auth/google/callback
```

---

## 🎯 Краткая версия:

```
Поле 1 (JavaScript origins):
http://localhost:3001

Поле 2 (Redirect URIs):
http://localhost:3001/api/auth/google/callback
```

**Скопируйте эти строки БЕЗ ПРОБЕЛОВ!**

---

## ✅ После создания:

1. Скопируйте **Client ID** и **Client Secret**
2. Вставьте в `.env.local`
3. Перезапустите сервер: `npm run dev`
4. Settings → Подключить Google
5. Готово! 🎉

---

**Просто скопируйте эти 2 строки в соответствующие поля!**




