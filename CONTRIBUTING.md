# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту! Мы рады любому вкладу.

## Как внести вклад

### 🐛 Сообщить об ошибке

1. Проверьте, что ошибка еще не была зарегистрирована
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версия браузера/ОС

### 💡 Предложить новую функцию

1. Создайте Issue с описанием:
   - Проблема, которую решает функция
   - Предлагаемое решение
   - Альтернативные варианты
   - Дополнительный контекст

### 📝 Внести изменения в код

1. Fork репозитория
2. Создайте ветку для вашей функции:
```bash
git checkout -b feature/amazing-feature
```

3. Внесите изменения и закоммитьте:
```bash
git commit -m "feat: add amazing feature"
```

4. Отправьте изменения:
```bash
git push origin feature/amazing-feature
```

5. Создайте Pull Request

## 📋 Стандарты кода

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`, используйте `unknown` при необходимости
- Создавайте интерфейсы для объектов

```typescript
// ✅ Хорошо
interface User {
  id: number
  name: string
}

// ❌ Плохо
const user: any = {}
```

### React компоненты

- Используйте функциональные компоненты
- Используйте TypeScript для props
- Добавляйте JSDoc комментарии для сложной логики

```typescript
interface ButtonProps {
  label: string
  onClick: () => void
  disabled?: boolean
}

export default function Button({ label, onClick, disabled }: ButtonProps) {
  return (
    <button onClick={onClick} disabled={disabled}>
      {label}
    </button>
  )
}
```

### Стилизация

- Используйте Tailwind CSS классы
- Для сложных стилей используйте MUI sx prop
- Следуйте дизайн-системе Telegram

```tsx
// ✅ Хорошо
<div className="flex items-center gap-2 p-4 bg-telegram-card">
  <Button sx={{ color: '#007aff' }}>Action</Button>
</div>

// ❌ Плохо
<div style={{ display: 'flex', padding: '16px' }}>
  <Button style={{ color: 'blue' }}>Action</Button>
</div>
```

### Именование

- **Компоненты**: PascalCase (`ChatMessage.tsx`)
- **Функции**: camelCase (`formatDate`)
- **Константы**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **Файлы**: kebab-case для utils (`date-utils.ts`)

### Commit messages

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, пропущенные точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: add calendar event creation
fix: resolve message scroll issue
docs: update deployment guide
```

## 🧪 Тестирование

Перед отправкой PR убедитесь, что:

1. Код компилируется без ошибок:
```bash
npm run build
```

2. Нет ошибок линтера:
```bash
npm run lint
```

3. Приложение работает локально:
```bash
npm run dev
```

4. Все функции работают в Telegram Mini App

## 📚 Документация

При добавлении новой функции:

1. Обновите README.md
2. Добавьте комментарии в код
3. Обновите FEATURES.md
4. Добавьте примеры использования

## 🎨 Дизайн

При изменении UI:

1. Следуйте дизайн-системе Telegram
2. Используйте темную тему
3. Проверьте адаптивность на разных экранах
4. Добавьте плавные переходы и анимации

## 🐞 Отладка

Для отладки используйте:

```typescript
// В dev режиме
if (process.env.NODE_ENV === 'development') {
  console.log('Debug info:', data)
}
```

Не забудьте удалить console.log перед коммитом!

## ⚡ Производительность

- Избегайте лишних ре-рендеров (используйте `useMemo`, `useCallback`)
- Оптимизируйте изображения
- Используйте lazy loading для больших компонентов
- Минимизируйте размер bundle

## 🔒 Безопасность

- Никогда не коммитьте секретные ключи
- Используйте переменные окружения
- Валидируйте пользовательский ввод
- Используйте HTTPS для всех запросов

## 📞 Связь

Если у вас есть вопросы:

1. Проверьте существующие Issues
2. Создайте новый Issue с вопросом
3. Свяжитесь с мейнтейнерами

## 📜 Лицензия

Внося вклад, вы соглашаетесь с тем, что ваш вклад будет лицензирован под MIT License.

---

Спасибо за ваш вклад! 🎉








