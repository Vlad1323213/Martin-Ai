# 🚀 Быстрый старт - Деплой на Vercel

## 1️⃣ Подготовка (5 минут)

### Создайте GitHub репозиторий

```bash
git init
git add .
git commit -m "Deploy Martin AI to Vercel"
git branch -M main
```

Создайте новый репозиторий на https://github.com/new

```bash
git remote add origin https://github.com/ваш-юзернейм/martin-ai.git
git push -u origin main
```

---

## 2️⃣ Деплой на Vercel (3 минуты)

1. Откройте https://vercel.com/new
2. Импортируйте ваш GitHub репозиторий
3. Vercel автоматически определит Next.js
4. Нажмите **"Deploy"**

⏳ Подождите 2-3 минуты...

✅ Получите ваш домен: `https://ваш-проект.vercel.app`

---

## 3️⃣ Настройка переменных окружения (5 минут)

### На Vercel Dashboard

**Settings → Environment Variables** → Добавьте:

```
GOOGLE_CLIENT_ID=ваш-клиент-id
GOOGLE_CLIENT_SECRET=ваш-секрет
GOOGLE_REDIRECT_URI=https://ваш-проект.vercel.app/api/auth/google/callback
NEXT_PUBLIC_GOOGLE_CALENDAR_API_KEY=ваш-api-ключ
```

⚠️ **ВАЖНО**: Замените `https://ваш-проект.vercel.app` на реальный домен!

Нажмите **"Save"**

---

## 4️⃣ Создание Vercel KV (Redis) - ОБЯЗАТЕЛЬНО! (3 минуты)

Без KV токены НЕ будут сохраняться!

1. **Storage** → **Create Database**
2. Выберите **"KV"**
3. Регион: **Washington, D.C., USA (iad1)**
4. Нажмите **"Create"**
5. **Connect Project** → Выберите ваш проект
6. Vercel добавит переменные автоматически:
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`
   - `KV_URL`

---

## 5️⃣ Обновление Google Cloud Console (5 минут)

### Авторизованные источники JavaScript

Добавьте:
```
https://ваш-проект.vercel.app
```

### Авторизованные URI перенаправления

Добавьте:
```
https://ваш-проект.vercel.app/api/auth/google/callback
```

⚠️ **Подождите 5-10 минут** для применения изменений!

---

## 6️⃣ Redeploy (1 минута)

После настройки KV и переменных:

**Vercel Dashboard → Deployments → ... (меню) → Redeploy**

---

## 7️⃣ Обновление Telegram Bot (2 минуты)

### В BotFather

```
/mybots → Ваш бот → Bot Settings → Menu Button → Edit Menu Button URL
```

Введите:
```
https://ваш-проект.vercel.app
```

---

## ✅ Готово!

Откройте бота в Telegram и проверьте:

- ✅ Приложение загружается
- ✅ OAuth работает (Settings → Подключить Google)
- ✅ Календарь показывает события
- ✅ Почта загружается

---

## 🐛 Troubleshooting

### "redirect_uri_mismatch"
- Проверьте что URL в `.env` на Vercel **ТОЧНО** совпадает с Google Cloud Console
- Убедитесь что нет пробелов
- Подождите 5-10 минут

### Токены не сохраняются
- Проверьте что KV база создана и подключена
- В логах должно быть: `✅ Tokens saved to KV` (а не "to memory")

### Ошибки сборки
- Проверьте логи: **Deployments → Ваш деплой → Logs**
- Очистите кэш: **Settings → General → Clear Build Cache**

---

## 📊 Проверка работы KV

В логах Vercel должно быть:

```
✅ Tokens saved to KV for user 123 (google)
✅ Tokens retrieved from KV for user 123 (google)
```

Если видите "memory" вместо "KV" - база не подключена!

---

🎉 **Ваш AI-агент в production!**

**Полная инструкция:** см. `DEPLOY.md`


