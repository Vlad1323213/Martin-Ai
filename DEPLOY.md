# 🚀 Деплой на Vercel

Инструкция по развертыванию Martin AI на Vercel.

## Шаг 1: Подготовка проекта

### 1.1 Убедитесь что проект собирается локально

```bash
npm run build
```

Если есть ошибки - исправьте их перед деплоем.

### 1.2 Проверьте `.gitignore`

Убедитесь что `.env.local` есть в `.gitignore`:

```
# Local env files
.env*.local
.env
```

---

## Шаг 2: Создание проекта на Vercel

### 2.1 Установите Vercel CLI (опционально)

```bash
npm i -g vercel
```

### 2.2 Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-юзернейм/martin-ai.git
git push -u origin main
```

### 2.3 Импортируйте проект на Vercel

1. Откройте https://vercel.com
2. Нажмите **"Add New Project"**
3. Импортируйте ваш GitHub репозиторий
4. Vercel автоматически определит Next.js

---

## Шаг 3: Настройка Environment Variables

### 3.1 Перейдите в Settings → Environment Variables

Добавьте следующие переменные:

#### Google OAuth (обязательно)

```
GOOGLE_CLIENT_ID=ваш-клиент-id
GOOGLE_CLIENT_SECRET=ваш-секрет
GOOGLE_REDIRECT_URI=https://ваш-домен.vercel.app/api/auth/google/callback
```

⚠️ **ВАЖНО**: После деплоя обновите `GOOGLE_REDIRECT_URI` на реальный домен Vercel!

#### Google Calendar API

```
NEXT_PUBLIC_GOOGLE_CALENDAR_API_KEY=ваш-api-ключ
```

#### Yandex OAuth (опционально)

```
YANDEX_CLIENT_ID=ваш-клиент-id
YANDEX_CLIENT_SECRET=ваш-секрет
YANDEX_REDIRECT_URI=https://ваш-домен.vercel.app/api/auth/yandex/callback
```

---

## Шаг 4: Настройка Vercel KV (Redis для хранения токенов)

### 4.1 Создайте Vercel KV базу

1. Откройте ваш проект на Vercel
2. Перейдите в **Storage**
3. Нажмите **"Create Database"**
4. Выберите **"KV" (Redis)**
5. Выберите регион (рекомендуется тот же что и ваше приложение)
6. Нажмите **"Create"**

### 4.2 Подключите KV к проекту

1. После создания базы, нажмите **"Connect Project"**
2. Выберите ваш проект
3. Vercel автоматически добавит переменные окружения:
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`
   - `KV_URL`

⚠️ **Эти переменные критически важны** - без них хранение токенов не будет работать в production!

---

## Шаг 5: Обновление Google Cloud Console

### 5.1 Авторизованные источники JavaScript

Добавьте ваш Vercel домен:

```
https://ваш-домен.vercel.app
```

### 5.2 Авторизованные URI перенаправления

Добавьте:

```
https://ваш-домен.vercel.app/api/auth/google/callback
```

### 5.3 Обновите переменную на Vercel

После получения домена, обновите `GOOGLE_REDIRECT_URI` в Vercel:

1. Settings → Environment Variables
2. Найдите `GOOGLE_REDIRECT_URI`
3. Измените на `https://ваш-домен.vercel.app/api/auth/google/callback`
4. Нажмите **"Save"**
5. **Redeploy** проект

---

## Шаг 6: Настройка Telegram Mini App

### 6.1 Обновите URL в BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. **Bot Settings** → **Menu Button**
5. **Edit Menu Button URL**
6. Введите: `https://ваш-домен.vercel.app`

### 6.2 Настройте Web App URL

1. **Bot Settings** → **Web App**
2. Введите: `https://ваш-домен.vercel.app`

---

## Шаг 7: Деплой!

### 7.1 Через Vercel Dashboard

Vercel автоматически задеплоит при push в main branch.

### 7.2 Через CLI

```bash
vercel --prod
```

---

## Шаг 8: Проверка деплоя

### 8.1 Откройте ваш домен

```
https://ваш-домен.vercel.app
```

### 8.2 Проверьте логи

Vercel Dashboard → Deployments → Ваш деплой → Logs

### 8.3 Проверьте OAuth

1. Откройте приложение в Telegram
2. Нажмите **Настройки** (шестеренка)
3. **Подключить Google**
4. Должно открыться окно авторизации

---

## Troubleshooting

### Ошибка: "redirect_uri_mismatch"

**Решение:**
1. Проверьте что `GOOGLE_REDIRECT_URI` в Vercel совпадает с Google Cloud Console
2. Убедитесь что НЕТ пробелов в начале/конце URL
3. Подождите 5-10 минут после изменений в Google Cloud Console

### Ошибка: "No KV_REST_API_URL or KV_REST_API_TOKEN"

**Решение:**
1. Создайте Vercel KV базу (Шаг 4)
2. Подключите её к проекту
3. Redeploy приложение

### Токены не сохраняются

**Решение:**
1. Проверьте что KV база подключена
2. Проверьте логи на Vercel:
   ```
   ✅ Tokens saved to KV for user...
   ```
3. Если видите "saved to memory" - KV не настроен

### Ошибка сборки

**Решение:**
1. Проверьте что проект собирается локально: `npm run build`
2. Проверьте версию Node.js (рекомендуется 18.x или 20.x)
3. Очистите кэш: Settings → General → Clear Build Cache

---

## Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel KV Docs](https://vercel.com/docs/storage/vercel-kv)
- [Google Cloud Console](https://console.cloud.google.com/)
- [Telegram BotFather](https://t.me/botfather)

---

## После деплоя

✅ **Checklist:**

- [ ] Приложение открывается по Vercel домену
- [ ] OAuth работает (Google авторизация)
- [ ] Токены сохраняются в KV (не "in memory")
- [ ] Календарь показывает события
- [ ] Почта загружается
- [ ] Приложение работает в Telegram

---

## Production рекомендации

### 1. Добавьте Custom Domain

Vercel → Settings → Domains → Add Domain

### 2. Настройте Analytics

Vercel → Analytics → Enable

### 3. Добавьте мониторинг ошибок

Рекомендуется Sentry:

```bash
npm install @sentry/nextjs
npx @sentry/wizard@latest -i nextjs
```

### 4. Настройте Rate Limiting

Для защиты от злоупотреблений API добавьте rate limiting в middleware.

### 5. Включите CORS (если нужно)

В `next.config.js`:

```js
async headers() {
  return [
    {
      source: '/api/:path*',
      headers: [
        { key: 'Access-Control-Allow-Origin', value: '*' },
      ],
    },
  ]
}
```

---

🎉 **Готово!** Ваш AI-агент теперь в production!


