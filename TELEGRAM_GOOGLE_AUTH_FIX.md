# 🔧 Исправление проблемы с Google авторизацией в Telegram Mini App

## 🎯 Проблема
Google авторизация не открывается или не работает в Telegram Mini App на Vercel

---

## ✅ Решение - Пошаговая инструкция

### Шаг 1: Проверьте переменные окружения на Vercel

1. Откройте **Vercel Dashboard** → ваш проект
2. Перейдите в **Settings → Environment Variables**
3. Убедитесь что есть ВСЕ эти переменные:

```env
GOOGLE_CLIENT_ID=ваш-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-ваш-secret
GOOGLE_REDIRECT_URI=https://ваш-домен.vercel.app/api/auth/google/callback
NEXT_PUBLIC_GOOGLE_CALENDAR_API_KEY=ваш-api-ключ
```

⚠️ **КРИТИЧЕСКИ ВАЖНО:**
- `GOOGLE_REDIRECT_URI` должен содержать **ТОЧНЫЙ** домен вашего Vercel проекта
- Проверьте что домен **БЕЗ пробелов** в начале или конце
- URL должен заканчиваться на `/api/auth/google/callback`

**Пример правильного URL:**
```
https://martin-ai-telegram.vercel.app/api/auth/google/callback
```

❌ **Неправильно:**
```
http://localhost:3001/api/auth/google/callback    (это для локальной разработки)
https://ваш-домен.vercel.app                      (нет пути /api/auth/google/callback)
```

---

### Шаг 2: Найдите ваш реальный Vercel домен

1. В **Vercel Dashboard** → **Deployments**
2. Нажмите на последний деплой
3. Скопируйте URL (например: `https://martin-ai-telegram.vercel.app`)

**Это и есть ваш домен для переменных окружения!**

---

### Шаг 3: Обновите Google Cloud Console

#### 3.1 Откройте Google Cloud Console
https://console.cloud.google.com/apis/credentials

#### 3.2 Найдите OAuth 2.0 Client ID
- Найдите ваш OAuth клиент
- Нажмите на его имя или иконку ✏️ (редактировать)

#### 3.3 Добавьте Vercel URLs

**Authorized JavaScript origins (Авторизованные источники JavaScript):**

Нажмите **"+ ADD URI"** и добавьте:
```
https://ваш-домен.vercel.app
```

⚠️ **Важно:** Только домен, БЕЗ пути `/api/...`

**Authorized redirect URIs (Авторизованные URI перенаправления):**

Нажмите **"+ ADD URI"** и добавьте:
```
https://ваш-домен.vercel.app/api/auth/google/callback
```

⚠️ **Важно:** С ПОЛНЫМ путем `/api/auth/google/callback`

#### 3.4 Итоговый результат должен быть:

**JavaScript origins:**
- ✅ `https://ваш-домен.vercel.app`
- ✅ `http://localhost:3001` (для локальной разработки, можно оставить)

**Redirect URIs:**
- ✅ `https://ваш-домен.vercel.app/api/auth/google/callback`
- ✅ `http://localhost:3001/api/auth/google/callback` (для локальной разработки, можно оставить)

#### 3.5 Сохраните
Нажмите **"SAVE"** внизу страницы

⏰ **Подождите 5-10 минут** после сохранения - Google нужно время на обновление!

---

### Шаг 4: Проверьте что KV база подключена

1. **Vercel Dashboard → Storage**
2. Убедитесь что есть KV база
3. Проверьте что она **подключена к вашему проекту**:
   - Нажмите на базу
   - **Connected Projects** → должен быть ваш проект

Если базы нет - создайте:
1. **Storage → Create Database → KV**
2. Регион: **Washington, D.C., USA (iad1)**
3. **Create**
4. **Connect Project** → выберите ваш проект

---

### Шаг 5: Обновите переменные на Vercel (если изменили)

Если вы обновили `GOOGLE_REDIRECT_URI`:

1. **Settings → Environment Variables**
2. Найдите `GOOGLE_REDIRECT_URI`
3. Нажмите **"Edit"**
4. Вставьте правильный URL: `https://ваш-домен.vercel.app/api/auth/google/callback`
5. Выберите **"Production, Preview, Development"**
6. **Save**

---

### Шаг 6: Redeploy на Vercel

После ВСЕХ изменений:

1. **Vercel Dashboard → Deployments**
2. Найдите последний деплой
3. Нажмите **"..." (три точки)**
4. **"Redeploy"**

⏳ Подождите 2-3 минуты пока пройдет деплой

---

### Шаг 7: Проверьте работу

#### 7.1 Откройте в обычном браузере
```
https://ваш-домен.vercel.app
```

- Приложение должно загрузиться
- Откройте DevTools (F12) → Console
- Проверьте нет ли ошибок

#### 7.2 Проверьте OAuth в браузере
1. Нажмите **Settings** (шестеренка)
2. **"Подключить Google"**
3. Должно открыться окно Google OAuth
4. Войдите в аккаунт
5. Должно показать "Подключение успешно!"

**Если на этом шаге работает - значит OAuth настроен правильно!**

#### 7.3 Проверьте в Telegram

1. Откройте Telegram
2. Найдите вашего бота
3. Откройте Mini App
4. **Settings → Подключить Google**
5. Должен открыться браузер с Google OAuth
6. После входа вернитесь в Telegram
7. В Settings должно показать "✓ Подключено"

---

## 🐛 Типичные проблемы и решения

### Проблема 1: "redirect_uri_mismatch"

**Причина:** URL в Google Console не совпадает с GOOGLE_REDIRECT_URI

**Решение:**
1. Откройте Google Cloud Console
2. Проверьте что redirect URI **ТОЧНО** такой же как в переменных Vercel
3. Проверьте НЕТ лишних пробелов
4. Подождите 5-10 минут после сохранения
5. Redeploy на Vercel

### Проблема 2: OAuth окно не открывается в Telegram

**Причина:** Telegram не может открыть внешний браузер

**Решение:**
1. Проверьте что используется `telegramWebApp.openLink(url)` (уже есть в коде)
2. Убедитесь что URL правильный:
   ```
   https://ваш-домен.vercel.app/api/auth/google?userId=123
   ```
3. В некоторых версиях Telegram нужно дать разрешение на открытие браузера

### Проблема 3: Токены не сохраняются

**Причина:** KV база не подключена

**Решение:**
1. Создайте KV базу (Шаг 4)
2. Подключите к проекту
3. Redeploy

**Проверка в логах Vercel:**
- ✅ Хорошо: `✅ Tokens saved to KV for user...`
- ❌ Плохо: `Tokens saved to memory`

### Проблема 4: После OAuth не возвращается в Telegram

**Причина:** Telegram username бота не настроен правильно

**Решение:**

В файле `app/api/auth/google/callback/route.ts` (строка 140) проверьте:
```javascript
const telegramUrl = 'tg://resolve?domain=martinaibot';
```

Замените `martinaibot` на **username вашего бота** (без @):

Например, если ваш бот `@myaibot`, то:
```javascript
const telegramUrl = 'tg://resolve?domain=myaibot';
```

После изменения:
```bash
git add .
git commit -m "Update telegram bot username"
git push
```

Vercel автоматически задеплоит новую версию.

---

## 📊 Проверка логов Vercel

1. **Vercel Dashboard → Deployments**
2. Нажмите на последний деплой
3. **"Runtime Logs"**

**Что должно быть:**
```
✅ Tokens saved to KV for user 123456 (google)
✅ Connection check for user 123456: Google=true, Yandex=false
```

**Чего НЕ должно быть:**
```
❌ Tokens saved to memory
❌ Google OAuth error: redirect_uri_mismatch
❌ Failed to check connections
```

---

## 🎯 Чек-лист проверки

Перед тестированием убедитесь что:

- [ ] В Vercel есть ВСЕ переменные окружения
- [ ] `GOOGLE_REDIRECT_URI` содержит правильный Vercel домен
- [ ] В Google Console добавлены Vercel URLs
- [ ] В Google Console URLs БЕЗ пробелов
- [ ] KV база создана и подключена к проекту
- [ ] После изменений сделан Redeploy
- [ ] Прошло 5-10 минут после изменений в Google Console
- [ ] В логах Vercel нет ошибок
- [ ] OAuth работает в обычном браузере
- [ ] Telegram bot username правильный в callback/route.ts

---

## 📱 Дополнительная настройка Telegram Bot

### Убедитесь что Menu Button настроен:

1. Откройте **@BotFather** в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. **Bot Settings → Menu Button → Edit Menu Button URL**
5. Вставьте:
   ```
   https://ваш-домен.vercel.app
   ```

---

## 🎉 Готово!

После выполнения всех шагов Google авторизация должна работать в Telegram Mini App!

**Если проблема остается:**
1. Проверьте логи Vercel
2. Проверьте консоль браузера (F12)
3. Попробуйте в приватном окне браузера
4. Убедитесь что ваш email добавлен в Test Users в Google Cloud Console

---

## 🆘 Нужна помощь?

Дополнительная документация:
- `QUICKSTART_VERCEL.md` - полная инструкция по Vercel
- `GOOGLE_OAUTH_FIX.md` - решение проблем с OAuth
- `DEPLOY_SUMMARY.md` - итоговая сводка деплоя

**Удачи! 🚀**

