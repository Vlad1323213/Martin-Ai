# 🌐 Запуск туннеля для Telegram Mini App

Для тестирования приложения в Telegram необходимо создать публичный HTTPS URL для вашего локального сервера.

## Быстрый запуск (Windows)

1. Убедитесь, что сервер Next.js запущен на порту **3001**:
```bash
npm run dev
```

2. Просто **двойной клик** на файл:
```
start-tunnel.bat
```

3. В открывшемся окне **скопируйте URL** вида:
```
your url is: https://xxxxxx.loca.lt
```

4. Этот URL используйте в **BotFather** при настройке Telegram Mini App

## Альтернативный запуск (командная строка)

```bash
npx localtunnel --port 3001
```

## Важно! ⚠️

### При первом запуске
- LocalTunnel может попросить **подтверждение в браузере**
- Просто откройте полученный URL в браузере
- Нажмите кнопку **"Continue"** или **"Click to Continue"**
- После этого URL будет работать в Telegram

### Особенности
- ✅ **Бесплатно** и без регистрации
- ✅ **Быстрый запуск** - не требует установки
- ⚠️ URL **меняется** при каждом перезапуске туннеля
- ⚠️ Туннель работает только **пока открыто окно** с запущенным процессом

### Если URL изменился
Когда вы перезапускаете туннель, URL меняется. Нужно:
1. Скопировать **новый URL**
2. Обновить его в **BotFather**:
   - Откройте @BotFather
   - Отправьте `/myapps`
   - Выберите свое приложение
   - **Edit Bot** → **Edit Web App URL**
   - Вставьте новый URL

## Альтернативные туннели

### ngrok (если LocalTunnel не работает)

```bash
# Установка
npm install -g ngrok

# Запуск
ngrok http 3001
```

### serveo.net (через SSH)

```bash
ssh -R 80:localhost:3001 serveo.net
```

## Проблемы и решения

### "Unable to connect"
- Убедитесь, что сервер Next.js запущен на порту 3001
- Проверьте: `npm run dev` должен показывать `Local: http://localhost:3001`

### "Error 1033" или "Tunnel error"
- Перезапустите туннель
- Попробуйте другой туннель-сервис (ngrok, serveo)

### Туннель запустился, но нет URL
- Подождите 10-15 секунд
- URL появится в окне автоматически

---

**Готово!** Теперь ваше приложение доступно через публичный URL! 🎉






