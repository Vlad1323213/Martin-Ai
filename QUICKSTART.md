# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Шаг 3: Просмотр в Telegram

### Локальная разработка (с туннелем)

Для тестирования в Telegram вам нужен публичный URL. Используйте один из вариантов:

#### Вариант A: LocalTunnel (рекомендуется)

```bash
# В одном терминале запустите Next.js
npm run dev

# В другом терминале создайте туннель
# Или просто запустите start-tunnel.bat
npx localtunnel --port 3001
```

Скопируйте HTTPS URL из localtunnel (например, `https://abc123.loca.lt`)

**Для Windows**: Просто запустите `start-tunnel.bat` двойным кликом!

#### Вариант B: ngrok

```bash
# Установите ngrok
npm install -g ngrok

# Создайте туннель
ngrok http 3001
```

### Настройка Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота: `/newbot`
3. Следуйте инструкциям и получите токен
4. Настройте Mini App: `/newapp`
5. Выберите вашего бота
6. Введите название приложения: `Martin AI`
7. Введите описание
8. Загрузите иконку (512x512px)
9. Введите Web App URL: ваш localtunnel URL
10. Выберите Short name: `martinai` (или другое)

### Открытие в Telegram

После настройки откройте Mini App через:
- Прямая ссылка: `https://t.me/YOUR_BOT_NAME/YOUR_SHORT_NAME`
- Или через чат с ботом кнопка "Open Mini App"

## Шаг 4: Сборка для продакшена

```bash
npm run build
npm start
```

## Быстрый деплой на Vercel

1. Зарегистрируйтесь на [Vercel](https://vercel.com)
2. Установите Vercel CLI:
```bash
npm install -g vercel
```

3. Деплой:
```bash
vercel
```

4. Используйте полученный URL в настройках Telegram бота

## Структура компонентов

### Основные компоненты:

- `ChatMessage` - Отображение сообщений в чате
- `ActionCard` - Интерактивные карточки с действиями
- `BottomBar` - Панель быстрых действий
- `InputBar` - Поле ввода сообщений
- `Header` - Шапка приложения
- `TypingIndicator` - Индикатор печати AI

### API Routes:

- `/api/chat` - Обработка сообщений AI
- `/api/todos` - CRUD для задач
- `/api/calendar` - Управление событиями календаря
- `/api/emails` - Получение и управление письмами

## Кастомизация

### Изменение цветов

Отредактируйте `tailwind.config.ts`:

```typescript
colors: {
  'telegram-bg': '#ВАШ_ЦВЕТ',
  'telegram-card': '#ВАШ_ЦВЕТ',
  // ...
}
```

### Изменение темы MUI

Отредактируйте `app/providers/ThemeProvider.tsx`:

```typescript
const darkTheme = createTheme({
  palette: {
    primary: {
      main: '#ВАШ_ЦВЕТ',
    },
    // ...
  },
})
```

## Интеграция с реальными сервисами

### Google Calendar

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com)
2. Включите Google Calendar API
3. Создайте OAuth 2.0 credentials
4. Добавьте credentials в `.env.local`:
```
GOOGLE_CLIENT_ID=ваш_client_id
GOOGLE_CLIENT_SECRET=ваш_client_secret
```

### Gmail API

Аналогично Google Calendar, но включите Gmail API

### OpenAI для реального AI

1. Получите API ключ на [OpenAI](https://platform.openai.com)
2. Добавьте в `.env.local`:
```
OPENAI_API_KEY=ваш_api_ключ
```

3. Раскомментируйте код в `app/api/chat/route.ts`

## Полезные ссылки

- [Next.js Documentation](https://nextjs.org/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Material UI](https://mui.com)
- [Tailwind CSS](https://tailwindcss.com)

## Возможные проблемы

### Проблема: "Telegram is not defined"

**Решение**: Убедитесь, что Telegram SDK загружен. Проверьте файл `public/telegram-web-app.js`

### Проблема: Приложение не открывается в Telegram

**Решение**: 
- Проверьте, что URL использует HTTPS
- Убедитесь, что URL правильно настроен в BotFather
- Попробуйте очистить кеш Telegram

### Проблема: Стили не применяются

**Решение**: 
```bash
# Очистите .next и пересоберите
rm -rf .next
npm run dev
```

## Следующие шаги

1. ✅ Интегрируйте реальный AI API
2. ✅ Подключите базу данных (PostgreSQL, MongoDB)
3. ✅ Добавьте аутентификацию пользователей
4. ✅ Интегрируйте Google Calendar
5. ✅ Интегрируйте Gmail API
6. ✅ Добавьте push-уведомления
7. ✅ Улучшите AI промпты

---

Удачи в разработке! 🎉



